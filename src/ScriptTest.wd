void entry() -> {
    const string text -> 'Third'
    const number FinalNumber -> 1
    number bNUm -> 55.3

    ThirdFunction(text, A: FinalNumber, C: bNUm)
}

void ThirdFunction(text, C, A) -> {
    out(text)
    out(A)
    out(C)
}